<div class="row">
  <h1>Welcome, <%=@user.name%>!</h1>
  <%= link_to "Raise a sticky", new_sticky_path %>


  <h3>Your stickies</h3>
  <ul id="sticky-list" class="small-block-grid-3">
    <% @stickies.each do |sticky| %>
      <li class="sticky yellow"><%=link_to sticky.title, sticky_path(sticky) %>
      	<% if sticky.user.id == session[:user_id] %><br>
      		<%=link_to "Delete",sticky_path(sticky), method:"delete" ,class:"label" %>
          <%= link_to "Edit", edit_sticky_path, class:"label", data: {confirm: "Whoa!"} %>
      	<% end %>

      </li>
    <% end %>
  </ul>
</div>

